<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.thoughtworks.exam.core.LogicQuestionRuleRepository">
    <resultMap id="logicQuestionRuleMapper" type="LogicQuestionRule">
        <id property="id" column="id" />
        <result property="level" column="level"/>
        <result property="amount" column="amount" />
        <result property="repositoryId" column="repo_id"/>
        <result property="repositoryName" column="repo_name"/>
        <result property="repositoryUrl" column="repo_url"/>
    </resultMap>

    <select id="findLogicQuestionRules" resultMap="logicQuestionRuleMapper">
        select rule.*,repo.name as repo_name,repo.url as repo_url from LogicQuestionRule rule, LogicQuestionRepo repo
        where repo.id = rule.repo_id;
    </select>

    <insert id="createLogicQuestionRule">
        <selectKey keyProperty="id" resultType="String" order="BEFORE">
            select uuid() from dual;
        </selectKey>
        insert into LogicQuestionRule(id,level,amount,repo_id) values(#{id,jdbcType=CHAR},#{level},#{amount},#{repositoryId});
    </insert>

    <select id="getLogicQuestionRuleById" resultMap="logicQuestionRuleMapper">
        select rule.*,repo.name as repo_name,repo.url as repo_url from LogicQuestionRule rule, LogicQuestionRepo repo
        where rule.id = #{id} and repo.id = rule.repo_id;
    </select>

    <select id="getLogicQuestionRulesByTemplateId" resultMap="logicQuestionRuleMapper">
        select rule.*, repo.name as repo_name, repo.url as repo_url from LogicQuestionRule rule, LogicQuestionRepo repo
        where rule.template_id = #{templateId} and repo.id = rule.repo_id;
    </select>
</mapper>